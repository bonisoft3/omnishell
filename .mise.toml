[settings]
locked = true
github.slsa = false
github.github_attestations = false
lockfile = true
experimental = true
paranoid = false
github_attestations = false
slsa = false
aqua.github_attestations = false
aqua.cosign = false
aqua.slsa = false
aqua.minisign = false

[tools]
# github backend instead of mise's core bun plugin: the core plugin
# always picks the musl variant, which fails on bayt.nubox's leap base
# (glibc). github backend with mise >= 2026.5.2 (bundled in lazybox
# v0.8.3) correctly picks bun-linux-x64.zip for glibc.
"github:oven-sh/bun" = "bun-v1.3.12"
# interpreter/*-smoke.js run under deno, not bun: they lock down SES and need
# deno's own permission flags and lockfile.
"github:denoland/deno" = "v2.3.7"
